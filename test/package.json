{
  "name": "cycript-test",
  "version": "1.0.0",
  "description": "Test harness used internally by Cycript",
  "private": true,
  "main": "types.js",
  "scripts": {
    "test": "DYLD_LIBRARY_PATH=$(pwd)/../src/.libs node node_modules/mocha/bin/_mocha"
  },
  "license": "MIT",
  "devDependencies": {
    "bindings": "^1.2.1",
    "mocha": "^3.0.2",
    "nan": "^2.4.0",
    "should": "^11.1.0"
  }
}
