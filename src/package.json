{
  "name": "cycript-agent",
  "version": "1.0.0",
  "description": "Frida agent used internally by Cycript",
  "private": true,
  "main": "Agent/index.js",
  "scripts": {
    "build": "frida-compile Agent -o libcycript.qjs -bc",
    "watch": "frida-compile Agent -o libcycript.qjs -bw"
  },
  "license": "MIT",
  "devDependencies": {
    "frida": "^14.0.5",
    "frida-compile": "^10.0.0",
    "mjolner": "^4.0.2"
  }
}
